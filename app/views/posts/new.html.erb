<h1>New Posts</h1>
<%= form_with(model: @post, local: true) do |f| %>
    <% if @post.errors.present? %>
        <p class="form-errors">
            <%= @post.errors.full_messages.join(", ") %>
        </p>
    <% end %>
    <div>
        <%= f.label :title %>
        <%= f.text_field :title %>
    </div>
    <div>
        <%= f.label :body %>
        <%= f.text_area :body %>
    </div>
    <%= f.submit %>
<% end %>
